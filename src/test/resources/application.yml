spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:agentworks;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  sql:
    init:
      mode: always

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
